import {MapPin,CheckCircle2,LoaderCircle} from 'lucide-react';
export default function LocationButton({location,loading,error,onGetLocation}){return <div className="location-area"><button type="button" className={`location-button ${location?'location-ready':''}`} onClick={onGetLocation} disabled={loading}>{loading?<LoaderCircle className="spin" size={18}/>:location?<CheckCircle2 size={18}/>:<MapPin size={18}/>} {loading?'Detecting location...':location?'Location ready':'Use my current location'}</button>{location&&<span className="coordinates">{location.latitude.toFixed(5)}, {location.longitude.toFixed(5)}</span>}{error&&<p className="field-error">{error}</p>}</div>}
