import {Search,LoaderCircle} from 'lucide-react';
export default function QueryForm({query,onQueryChange,onSubmit,loading,disabled}){return <form onSubmit={onSubmit}><label htmlFor="medical-query" className="input-label">Describe your health concern</label><textarea id="medical-query" value={query} onChange={e=>onQueryChange(e.target.value)} placeholder="Example: I have irregular heartbeats and racing heartbeat" rows="5" maxLength="2000" disabled={loading}/><div className="form-footer"><span className="character-count">{query.length}/2000</span><button type="submit" className="primary-button" disabled={disabled||loading}>{loading?<><LoaderCircle className="spin" size={18}/>Finding specialists...</>:<><Search size={18}/>Find Specialist</>}</button></div></form>}
